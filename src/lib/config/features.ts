/**
 * Switches for parts of the redesign that are built but not ready to show.
 * Each one says what it is waiting for, so nobody has to guess whether it is
 * safe to turn on.
 */

/**
 * The Tienda block on Inicio. Off until we have the real copy (where the
 * money goes, shipping, pickup) and a destination URL. The mockup ships with
 * placeholder text that must not reach anybody.
 */
export const SHOW_TIENDA = false;

/** Where "Ver la tienda" points once SHOW_TIENDA is on. */
export const TIENDA_URL = '';
